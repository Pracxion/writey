DISCORD_TOKEN=
# Server Id (optional: registers slash commands to this guild instantly instead of globally)
GUILD_ID=
RUST_LOG=info
# Use system Opus library instead of building from source
OPUS_STATIC=0
